<form method="post" action="{{ path('delete_out_of_stock', {'id': out_of_stock.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ out_of_stock.id) }}">
    <button class="btn btn-outline-dark btn-sm">Supprimer</button>
</form>
