{% block body %}

    <style>
        .table-bordered {
            border-collapse: collapse;
        }

        .table-bordered th,
        .table-bordered td {
            border: 1px solid black;
            padding: 5px;
        }
    </style>

    <div class="export-product">
        <h1>Stock au {{ "now"|date("d/m/Y") }}</h1>
        <table class="table table-bordered">
            <thead>
            <tr>
                <th>Marque</th>
                <th>Désignation</th>
                <th>Référence</th>
                <th>Numéro de série</th>
                <th>Date d'ajout</th>
            </tr>
            </thead>
            <tbody>
            {% for item in data %}
                <tr>
                    <td>{{ item.brand | title }}</td>
                    <td>{{ item.name | title }}</td>
                    <td>{{ item.reference }}</td>
                    <td>{{ item.serialNumber }}</td>
                    <td style="text-align: center">{{ item.addedAt | date('d/m/y') }}</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}
