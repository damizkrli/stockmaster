<script>
    let titleText = [
    "STOCK",
    "HORS-STOCK",
    "FOURNITURES",
    "INVENTAIRE"
    ];
    let counter = 0;

    setInterval(function () {
    document.title = titleText[counter % titleText.length];
    counter++;
}, 2000)
</script>